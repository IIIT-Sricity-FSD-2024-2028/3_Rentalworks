version: '3.8'

services:
  db:
    image: postgres:16
    container_name: rentalworks_db
    environment:
      POSTGRES_USER: rentalworks
      POSTGRES_PASSWORD: rentalworks
      POSTGRES_DB: rentalworks_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: unless-stopped

volumes:
  postgres_data:
